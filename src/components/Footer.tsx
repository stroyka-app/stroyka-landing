/**
 * The one footer, on every route: Morning Bone's SiteFooter. Kept at this
 * path so every page's `import Footer from "@/components/Footer"` gets it.
 */
export { default } from "@/components/site/finale/SiteFooter";

# Enforce the Node version pinned in .nvmrc / package.json engines (22.x).
# Without this, npm only warns on a version mismatch and will happily write
# a lockfile that CI (Node 22 / npm 10) then rejects. engine-strict turns
# that silent drift into an immediate EBADENGINE error — run `nvm use` first.
engine-strict=true
